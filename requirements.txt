# ============================================
# 物理实验AI助教 —— 依赖清单
# ============================================
# 安装方式：pip install -r requirements.txt

# --- 核心依赖 ---
zhipuai>=2.0.0           # GLM5.2 API Python SDK

# --- 科学计算 ---
numpy>=1.24.0            # 数组与统计运算
scipy>=1.10.0            # curve_fit 数据拟合
matplotlib>=3.7.0        # 科学图表绘制
uncertainties>=3.1.7     # 自动不确定度传递（可选）
lmfit>=1.2.0             # 高级非线性拟合（可选）

# --- Web界面 ---
gradio>=4.0.0            # 交互式Web UI

# --- 报告生成 ---
python-docx>=0.8.11      # Word报告生成

# --- 音频处理（语音输入模块） ---
# faster-whisper>=1.0.0  # 本地语音识别（可选，需要时取消注释）
# sounddevice>=0.4.6     # 录音（可选）