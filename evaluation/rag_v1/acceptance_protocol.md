# 验收基线协议

1. `dev` 包含先前已查看的 56 题，可用于检索阈值、提示词和答案策略调整。
2. `test` 包含新增 15 题，在开发调整期间不运行，不根据其结果改参数。
3. 开发集通过后，固结题库、Schema、评分规则、提示词、检索器和资料清单的 SHA-256。
4. 固结后只运行一次端到端 `test/blind`，并保留原始回答、证据、自动报告和回归门禁结果。
5. 测试结果不用于继续调参；若验收失败，将该版标记为失败，下一轮需建立新的未见测试集。
6. 人工复核使用 `manual_review_template.csv`，重点检查物理事实、课程要求分类、第二优先级泄漏、示例数据冒充和缺输入时的编造。

当前状态：`test` 运行次数为 0，尚未执行最终验收。
